name: CI/CD Pipeline with Telegram Notification

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm install

      - name: 🚀 Build project
        run: npm run build

      # 📢 Telegram notification (always runs)
      - name: 📢 Telegram Notification
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          JOB_STATUS="${{ job.status }}"
          REPO="${{ github.repository }}"
          BRANCH="${{ github.ref_name }}"
          COMMIT="${{ github.sha }}"
          WORKFLOW="${{ github.workflow }}"
          COMMIT_SHORT=$(echo "$COMMIT" | cut -c1-7)

          # Check if secrets are set
          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
            echo "⚠️ Telegram secrets not configured. Skipping notification."
            echo "Please set TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID in repository secrets."
            exit 0
          fi

          if [ "$JOB_STATUS" == "success" ]; then
            MESSAGE="✅ <b>CI/CD SUCCESS</b>%0A%0A📦 Repository: <code>$REPO</code>%0A🌿 Branch: <code>$BRANCH</code>%0A🔖 Commit: <code>$COMMIT_SHORT</code>%0A⚙️ Workflow: <code>$WORKFLOW</code>"
          else
            MESSAGE="❌ <b>CI/CD FAILED</b>%0A%0A📦 Repository: <code>$REPO</code>%0A🌿 Branch: <code>$BRANCH</code>%0A🔖 Commit: <code>$COMMIT_SHORT</code>%0A⚙️ Workflow: <code>$WORKFLOW</code>"
          fi

          echo "Sending Telegram notification..."
          echo "Job Status: $JOB_STATUS"

          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=$MESSAGE" \
            -d "parse_mode=HTML")

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)

          echo "HTTP Response Code: $HTTP_CODE"
          echo "Response Body: $BODY"

          if [ "$HTTP_CODE" != "200" ]; then
            echo "❌ Telegram notification failed with HTTP code: $HTTP_CODE"
            echo "Full response: $BODY"
            echo ""
            echo "Common fixes:"
            echo "1. Verify TELEGRAM_BOT_TOKEN is correct (format: 123456789:ABCdefGHI...)"
            echo "2. Verify TELEGRAM_CHAT_ID is correct (numeric ID)"
            echo "3. Make sure the bot has permission to message the chat"
            exit 1
          else
            echo "✅ Telegram notification sent successfully!"
          fi
